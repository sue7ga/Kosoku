CREATE TABLE IF NOT EXISTS kosoku (
    id         INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
    f          VARCHAR(255),
    t          VARCHAR(255),
    c          VARCHAR(255),
    s          VARCHAR(255),
    sortBy     VARCHAR(255)
);
